mod testing::array_testing


let glob_array_1: std::Arr[std::BigInt] = std::Arr[std::BigInt](length=100_u64)
let glob_array_2: std::Arr[std::BigInt] = [std::BigInt]
let glob_array_3: std::Arr[std::BigInt] = [1, 2, 3, 4, 5, 6, 7]



cls RandomClsTestCls100 { }


fun test_array() -> std::Void {
    std::test_std_1(1, 2)

    let a = 1
    let b = 2
    let c = 3
    let mut x = [1, 2, 3]
    # let mut first = x.get(0)
    # first = "hello"

    let x = a
}
