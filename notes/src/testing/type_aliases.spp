mod testing::type_aliases


fun test_type_aliases(other: std::Vec[std::BigInt]) -> std::Void {
    use std::Bool as B
    use std::{BigInt, Str}

    let x: Str
    let y: B

    x = "hello world"
    y = true
}
