mod main


cls Point {
    x: std.U32
    y: std.U32
    z: std.U32
}


cls Vector {
    pos: Point
    dir: std.BigDec
}


cls A { }


fun test(a: std.Str) -> std.Bool { }


fun func(v: Vector) -> std.Void {
    case v == then
        Vector(pos=Point(y, ..), ..) { }
        Vector(pos=Point(x, y=1, z), ..) { }
        Vector(pos=Point(x, y, z=1), ..) { }
}


# let Vector(pos=A(), ..) = v
# let A() = v.pos
